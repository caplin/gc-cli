{
  "name": "gc-cli",
  "version": "0.0.0",
  "scripts": {
    "lint": "eslint .",
    "build": "npm run lint && npm run test && npm run compile",
    "test": "cd global-compiler && npm t",
    "precompile": "cd global-compiler && npm run precompile && cd .. && cd gc-cli && npm run precompile",
    "compile": "cd global-compiler && npm run compile && cd .. && cd gc-cli && npm run compile"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/caplin/gc-cli.git"
  },
  "dependencies": {
    "array-includes": "^1.0.5",
    "capitalize": "^0.5.0",
    "chalk": "^0.5.1",
    "immutable": "^3.0.3",
    "js-formatter": "git://github.com/briandipalma/js-formatter",
    "minimist": "^1.1.0",
    "recast": "^0.10.0",
    "through2": "^0.6.1",
    "vinyl-fs": "^1.0.0",
    "winston": "^0.9.0"
  },
  "devDependencies": {
    "@briandipalma/eslint-config-bdp": "^3.0.0",
    "babel": "^4.6.3",
    "del": "^1.1.1",
    "eslint": "^2.2.0",
    "mocha": "^2.1.0",
    "rimraf": "^2.3.2"
  },
  "preferGlobal": true,
  "bin": "./gc-cli/bin/gc-cli.js"
}
