{
  "name": "global-compiler",
  "version": "0.0.0",
  "scripts": {
    "test": "mocha",
    "prepublish": "npm run build",
    "start": "npm run build && node index.js",
    "watch": "watchy -sw src -- npm run build",
    "build": "traceur --dir src/ out/ --modules=commonjs --sourcemap"
  },
  "dependencies": {
    "ast-types": "^0.4.8",
    "minimist": "^0.2.0",
    "recast": "^0.6.10",
    "traceur": "0.0.58"
  },
  "devDependencies": {
    "mocha": "^1.20.1",
    "watchy": "~0.4.4"
  },
  "bin": "./bin/global-compiler-cli.js"
}
